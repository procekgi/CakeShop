@using CakeShop.Models
@model List<Pedido>
@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
<div class="row">
    <div class="col-md-12">
        <h3>Pedidos</h3>
        <hr />
    </div>
</div>
<div class="row">
    <div class="col-md-12">
        <div class="table-responsive">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Pedido</th>
                        <th>Status</th>
                        <th>Nome Cliente</th>
                        <th>Data de Pedido</th>
                        <th>Data Entrega</th>
                        <th>Quantidade de Itens</th>
                        <th>Valor Total (R$)</th>
                        <th>&nbsp;</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var pedido in Model)
                    {
                        <tr>
                            <td>@pedido.Id_Pedido</td>
                            <td>@ExtensionMethods.GetEnumDescription(pedido.Status)</td>
                            <td>@pedido.Cliente.Nome</td>
                            <td>@pedido.DataPedido</td>
                            <td>@pedido.DataEntrega</td>
                            <td>@string.Format("{0:N0}", pedido.Itens.Count)</td>
                            <td>@string.Format("{0:C2}", pedido.Itens.Sum(item => item.Preco * item.Quantidade))</td>
                            <td>
                                <a href="@Url.Action("VisualizarItens", "Pedido", new { @id = pedido.Id_Pedido })">Ver detalhes</a>
                            </td>
                            <td>
                               <button type="button" class="btn btn-danger">Finalizar pedido</button>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
